@model MyNote.MVC.Models.VM.CreateFiles

<div class="modal-header">
    <h5 class="modal-title" id="exampleModalLabel">Zapisz zdjęcie główne</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<form asp-action="CreateFiles" asp-controller="Notes" enctype="multipart/form-data" method="post">
    <div class="modal-body">
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Files" class="control-label">Pliki</label>
                    <input asp-for="Files" class="form-control" type="file" multiple />
                    <span asp-validation-for="Files" class="text-danger"></span>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <div>
            <a asp-action="Index" class="btn btn-secondary">Powrót</a>
            <input type="submit" value="Zapisz" class="btn btn-primary" />
        </div>
    </div>
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
